%h1= @pic.tittle
%p= @pic.description
%p= @pic.user.email
= link_to "Back", root_path
= link_to "Edit Pic", edit_pic_path
= button_to "Delete", pic_path, method: :delete, data: { confirm: "Are you sure?" }